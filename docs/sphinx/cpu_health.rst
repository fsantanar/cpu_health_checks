cpu_health Main Module
======================


This module contains the CPUCheck Class with its check methods and __init__ constructor,
and a main() function to work as a wrapper that instantiates the object and runs the tests.

.. automodule:: cpu_health_checks.cpu_health
  :members:

